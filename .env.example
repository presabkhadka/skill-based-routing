# Postgres (matches docker-compose.yml; host port 5434 to avoid clashing with a local Postgres)
DATABASE_URL="postgresql://postgres:postgres@localhost:5434/skill_routing?schema=public"

# API
API_PORT=4000
WEB_ORIGIN="http://localhost:5173"
